function total(){
    var x=document.getElementById("x").value;
    var y=document.getElementById("y").value;
    var total=Number(x)+Number(y);/*Give meaning to xy and add*/
    document.getElementById("result").innerHTML = total;
    {
    var x=document.getElementById("x").value;
    if(x==""||isNaN(x))
    
    {
    
    alert("No meaning");//If the input is not a number, it will pop up
    //Marco xiangenyu
    }
    }
    }